{% extends "@Contao/content_element/_base.html.twig" %}

{% block content %}
    <div class="faq-item">
        <h3 class="faq-question">{{ question|raw }}</h3>
        <div class="faq-answer">{{ answer|raw }}</div>
    </div>
{% endblock %}
