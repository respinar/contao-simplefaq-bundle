{% extends "@Contao/content_element/_base.html.twig" %}

{% block content %}
    <div class="faq-container">
        {% if headline %}
            <{{ hl }} class="faq-title">{{ headline }}</{{ hl }}>            
        {% endif %}
        <div class="faq-items">
{% endblock %}