{% set attributes = attrs(attributes|default)
    .setIfExists('id', element_html_id)
    .addClass([element_css_classes, "content-simplefaq"])
%}

<div {% block attributes %}{{ attributes }}{% endblock %}>
    {% if headline.text %}
        <{{ headline.tag_name }} class="simplefaq__title">
            {{ headline.text|insert_tag_raw }}
        </{{ headline.tag_name }}>
    {% endif %}          
    <div class="simplefaq__items">
